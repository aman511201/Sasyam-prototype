# ==============================================================================
# SASYAM (AGRIFARMER) BACKEND CONFIGURATION
# ==============================================================================

# Server settings
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=development

# Allowed CORS Origins (comma separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# Database URL (Optional - defaults to SQLite or in-memory for dev)
# DATABASE_URL=postgresql://user:password@localhost:5432/sasyam_db

# Optional AI / LLM Keys (For server-side AI processing)
# GEMINI_API_KEY=
# OPENAI_API_KEY=

# Government e-NAM / Data.gov.in API
# DATA_GOV_IN_API_KEY=
